# Task

Create a file called permutations.py and define a function of the same name, permutations(array), which accepts a list of elements. The function will return a list of all possible permutations of these elements. 
